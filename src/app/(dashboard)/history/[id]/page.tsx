import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, Plus, Trash2 } from "lucide-react";
import { addEventAction, deleteEventAction } from "@/app/actions";
import { RunEditForm } from "@/components/forms";
import { DemoBanner, StatusBadge } from "@/components/ui";
import { getAppData, getRun } from "@/lib/data";
import { formatDate, formatDuration } from "@/lib/format";
import { progressName } from "@/lib/game-config";

export const dynamic="force-dynamic";
export default async function RunDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const [{run,events,isDemo},data]=await Promise.all([getRun(id),getAppData()]);if(!run)notFound();return <>{isDemo&&<DemoBanner/>}<Link href="/history" className="mb-6 inline-flex items-center gap-2 text-sm font-bold text-[#83d99a]"><ArrowLeft size={16}/> Volver al historial</Link><section className="panel grid-pattern mb-6 p-6 sm:p-8"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="eyebrow">Detalle del intento</p><h1 className="mt-2 text-4xl font-black">TRY #{run.try_number}</h1></div><StatusBadge status={run.status}/></div><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div><p className="text-xs uppercase text-[#69766e]">Duración</p><strong className="mt-1 block text-xl">{formatDuration(run.duration_seconds)}</strong></div><div><p className="text-xs uppercase text-[#69766e]">Progreso</p><strong className="mt-1 block text-xl">{progressName(run.progress_stage)}</strong></div><div><p className="text-xs uppercase text-[#69766e]">Muerte</p><strong className="mt-1 block text-xl">{run.dead_player?.name??"—"}</strong></div><div><p className="text-xs uppercase text-[#69766e]">Causa · dimensión</p><strong className="mt-1 block text-xl">{run.death_cause??"—"} · {run.dimension??"—"}</strong></div></div>{run.comment&&<blockquote className="mt-6 border-l-2 border-[#66d285] pl-4 text-[#b7c2ba]">“{run.comment}”</blockquote>}<p className="mt-5 text-xs text-[#647067]">Inicio: {formatDate(run.started_at)}{run.ended_at?` · Término: ${formatDate(run.ended_at)}`:""}</p></section>
  <div className="grid gap-6 lg:grid-cols-[.85fr_1.15fr]"><section><div className="mb-4"><p className="eyebrow">Bitácora</p><h2 className="mt-2 text-2xl font-black">Timeline</h2></div><div className="panel p-5">{events.length?<ol className="relative space-y-5 border-l border-[#344038] pl-5">{events.map(event=><li key={event.id} className="relative"><span className="absolute -left-[1.58rem] top-1 size-2 rounded-full bg-[#69d78a] ring-4 ring-[#172019]"/><p className="font-bold">{event.title}</p><p className="mt-1 text-xs text-[#748078]">{formatDate(event.happened_at)}</p><form action={deleteEventAction} className="mt-2"><input type="hidden" name="eventId" value={event.id}/><button disabled={isDemo} className="inline-flex items-center gap-1 text-xs text-[#b86b6e]"><Trash2 size={13}/> Eliminar</button></form></li>)}</ol>:<p className="py-6 text-center text-sm text-[#748078]">Aún no hay eventos en este TRY.</p>}<form action={addEventAction} className="mt-5 flex gap-2 border-t border-[#29322b] pt-5"><input type="hidden" name="runId" value={run.id}/><input className="input" name="title" required maxLength={120} placeholder="Entramos al Nether…"/><button disabled={isDemo} className="btn btn-primary" aria-label="Agregar evento"><Plus size={18}/></button></form></div></section><section><div className="mb-4"><p className="eyebrow">Correcciones</p><h2 className="mt-2 text-2xl font-black">Editar TRY</h2></div><RunEditForm run={run} players={data.players} disabled={isDemo}/></section></div></>}
